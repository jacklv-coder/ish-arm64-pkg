# SwiftPM
.build/
.swiftpm/
DerivedData/
Package.resolved
*.xcodeproj/xcuserdata/

# Native build outputs
build/
build-host/
build-check/
*.o
*.a

# Locally built guest test blobs (binary inputs for c-tests)
c-tests/blobs/*.bin

# macOS
.DS_Store

# Don't commit fakefs trees / xcframeworks; they live on Releases.
*.xcframework/
fs.tar.gz
